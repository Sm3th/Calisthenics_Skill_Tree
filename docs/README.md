# Screenshots

Add a screenshot of the app here named `screenshot.png` — the root README
references `./docs/screenshot.png`.

Capture it from `npm run dev` once the tree is populated with some mastered
skills so the lime connectors and progress bars are visible.
